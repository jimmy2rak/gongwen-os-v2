# 2026-07-06

## 修复 DOCX 导出首行缩进（"0.94厘米" + 居中元素也缩进）
- 根因：live 路由 `gongwen-os-v2/src/app/api/export/route.ts` 用 `indent: { firstLine: Math.round(convertInchesToTwip(0.37)) }` = 533twips = 0.94cm，且套到**所有段落**（含居中主标题/H1），同时单位用 twips 导致 Word 显示"厘米"而非"字符"。之前会话把"修复"写到了死代码 `GongWen-OS/src/lib/document/export-docx.ts`，所以一直无效。
- 修复（严格对齐 editor.css）：
  - 主标题/H1：移除 indent（居中无缩进），行距改 44pt。
  - H2/H3/H4/H5：改用 `firstLineChars: 200`（2字符），行距 36pt。
  - p/div 正文：`firstLineChars: 200` + 两端对齐(justified)，行距 28pt。
  - li：`left: 640`(2em) 左缩进，无首行缩进。
  - blockquote：`left: 960`(3em) 左缩进，无首行缩进。
  - 删除废弃的 `convertInchesToTwip` 导入与 `firstLineIndent` 常量，新增 `firstLineChars2/headingLineSpacing/quoteLeftIndent/listLeftIndent`。
- 验证：用本地 docx 包生成测试 docx 并解压，确认居中段落无 `<w:ind>`、正文为 `<w:ind w:firstLineChars="200"/>`、无 `w:firstLine=`。dev 服务器(next-server v16.2.10)运行中，热重载生效。

## 行距统一为 28pt（仅改行距，其余不动）
- 需求：除小标宋字体外的所有字体保持行距 28pt，包括标题1234（h1/h2/h3/h4）；同步编辑器预览与导出 docx。经用户确认：h1（公文大标题，方正小标宋）也改 28pt，**仅红头 `.red-header-text` 保持原行距 2.0**。
- 改 `editor.css`：h1 `line-height:2.0`→`28pt`；h2/h3/h4/h5 `line-height:2.25`→`28pt`；blockquote 新增 `line-height:28pt`（与导出一致）；p/li 原已 28pt 不动；`.red-header-text`(line 259) 保持 2.0 不动。
- 改 `src/app/api/export/route.ts`：删除 `titleLineSpacing`(44pt) 与 `headingLineSpacing`(36pt) 两个常量，全部 `spacing` 统一指向 `bodyLineSpacing`(28pt EXACT)；主标题 `title` 参数、content h1、h2、h3、h4/h5 现在全 28pt。红头(div)仍走 else 分支=bodyLineSpacing(28pt)，保持原样未动。
- 验证：用同库生成 6 段落(title/h1/h2/h3/h4/p) docx，解压确认所有 `<w:spacing w:line="560" w:lineRule="exact"/>`，无 880(44pt)/720(36pt) 残留。dev 服务器热重载生效。

## 标题按钮重新映射（题/T/H1-H4）
- 问题：小标宋主标题在工具栏亮起"T"（正文按钮），语义混乱。用户要求重新分配。
- 新建 `extensions/DocTitle.ts`：可编辑块级节点 `docTitle`，渲染为 `div[data-type=doc-title]`，小标宋居中28pt。
- **EditorToolbar.tsx**：
  - 新增"题"按钮(Heading icon) → `cmd(editor).setDocTitle()` (toggle docTitle 节点)
  - T 按钮(Type icon) → 改为"正文（仿宋）"，`clearNodes()` 回退到段落
  - H1 → 黑体 `toggleHeading({level:1})`
  - H2 → 楷体 `toggleHeading({level:2})`
  - H3 → 仿宋三级 `toggleHeading({level:3})`
  - H4 → 仿宋四级 `toggleHeading({level:4})`
  - 删除旧 H5(level:5)
- **editor.css**：新增 `.doc-title`(小标宋居中28pt)；h1→黑体、h2→楷体、h3→仿宋粗体、h4→仿宋；删除旧h1小标宋样式。
- **DocEditor.tsx**：注册 DocTitle 扩展；StarterKit heading levels [1,2,3,4]；TextAlign types 加 docTitle；templateMarkdownToHtml 中 `#`→doc-title、`##`→h1、`###`→h2。
- **page.tsx**：默认内容 `<h1>` 改为 `<div data-type="doc-title">`。
- **export/route.ts**：blockPattern 匹配 div+doc-title 属性；doc-title→小标宋居中无缩进；h1→黑体、h2→楷体、h3→仿宋粗体、h4→仿宋；删除 h5 分支。
- **ExportMenu.tsx**：PDF 打印样式同步（.doc-title 小标宋、h1 黑体、h2 楷体）。

## 小标宋标题行距改回 32pt
- 用户验收标题按钮映射通过后，要求小标宋（题 doc-title + 主标题参数）行距从 28pt 恢复 32pt，其余（h1-h4、正文 28pt、红头 2.0）一律不动。
- route.ts 新增 `docTitleLineSpacing`（ptToTwip(32)=640twip EXACT），替换 doc-title 分支与 title 参数两处 spacing；其余仍用 bodyLineSpacing(28pt)。
- editor.css `.doc-title` line-height 28pt→32pt；`.red-header-text` 保持 2.0 未动。
- 验证：生成 docx 确认题+主标题 w:line=640，其余 w:line=560。

## 下一步规划（2026-07-06 晚，P0-P3+行距+标题映射全部验收通过）
- 下一步=既定路线 **P4 AI 能力**。代码核查现状：
  - `apiKeys` 表已建（AES-256-GCM 设计、6 厂商 deepseek/doubao/openai/qwen/kimi/glm、models JSON），但**无加解密工具 / 无配置接口 / 无配置 UI**。
  - 无任何 AI 流式调用代码；侧边栏已有 `/settings` `/templates` `/knowledge` `/hotspots` 入口；模板系统后端 API+页面已存在（之前建好待验收）。
- P4 拆分（P4-A 是 P4-B 前置）：
  - P4-A API Key 配置中心：`lib/crypto.ts`(AES-256-GCM, 主密钥 env) + `GET/POST/PUT/DELETE /api/settings/api-keys`(按 userId 隔离, 返回仅掩码) + `/settings` 下「模型配置」UI(6 厂商卡片预设) + `lib/ai/providers.ts` 预设表(baseURL/默认模型)。
  - P4-B 流式对话：`POST /api/ai/chat`(SSE 透传各家 OpenAI 兼容接口) + 公文 system prompt(`lib/ai/prompts.ts`) + 编辑器选中文字 AI 助手抽屉(续写/润色/缩写/扩写/解释/翻译/自定义, 插入/替换/复制) + 模型选择器(仅列已配置启用)。
  - P4-C 公文辅助生成：要素→初稿, 结合 RedHeader/DocNumber/Seal 自动补红头/发文字号/落款。
- 后续路线图：P5 模板接入新建流程+验收+要素生成；P6 WebDAV 云盘+AI面板+知识库+热点；P7 审阅 docx+md 双格式/版本覆盖。

## P4-A 实现完成（API Key 配置中心）
- `src/server/lib/crypto.ts`：AES-256-GCM 加解密，主密钥由 `AI_KEY_SECRET` 经 sha256 派生 32 字节；`encryptApiKey` 返回 base64(密文+authTag)+iv；`decryptApiKey` 拆分 authTag；`maskApiKey` 首尾掩码。
- `src/server/lib/ai/providers.ts`：6 厂商预设(deepseek/doubao/openai/qwen/kimi/glm)，含 baseURL、默认 models、openaiCompatible；`isValidProvider`/`getProvider` 校验。
- `src/server/db/schema/apiKeys.ts`：新增 `defaultModel` 列；并对 `data.db` 执行幂等 `ALTER TABLE api_keys ADD COLUMN default_model TEXT`（已执行）。
- `src/app/api/settings/api-keys/route.ts`：GET(仅掩码不回明文)/POST/PUT/DELETE，按 `getServerUser().id` 隔离，写入用 `client.execute`（与 documents/templates 一致）。
- `src/app/(dashboard)/settings/page.tsx`：新建「模型配置」页（Sidebar 已有入口但此前无页面），6 厂商卡片、粘贴 Key 自动掩码、模型多选、默认模型选择、启用开关、删除二次确认（内联浮层，沿用 templates 页约定，无独立 CustomDialog 组件）。
- `.env`：新增 `AI_KEY_SECRET`（已生成 32 字节 base64）。
- 验证：`tsc --noEmit` 0 错误；真实 data.db 跑通 加密→入库→读取→解密→掩码 全链路，default_model/is_active 列正确；dev 服务器热重载，未登录访问 `/api/settings/api-keys` 与 `/settings` 均 307 重定向至 /login（路由已注册且鉴权生效）。

## 系统设置二级菜单（本日后续）
- 用户要求「系统设置」内建二级菜单，API配置是其中一个，并预留 个人信息、菜单可见性 两项以后扩展。
- 重构：`src/app/(dashboard)/settings/page.tsx` 改为「左侧二级菜单 + 右侧内容区」壳；左侧 `SETTINGS_MENUS` 数组（id/label/desc/icon/ready）驱动，新增菜单项只需追加数组项 + 增加渲染分支。
- 原 API 配置全量抽离至 `src/components/settings/ApiConfigPanel.tsx`（逻辑逐字搬，无行为变更，避免回归）；页面壳中对 `active==="api"` 渲染该组件，其余 `ready=false` 项显示「开发中」占位。
- 验证：`tsc --noEmit` 0 错误；`curl /settings` 返回 200。
