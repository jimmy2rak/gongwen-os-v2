# 2026-07-09 开发日志

## 热点推送筛选修复

- **问题**：`CategoryFilterPills` 默认渲染公文类型（通知/报告/请示等），但热点推送栏目 pill 应显示真实 columnId（人民日报/新华网等）。
- **修复**：
  - `src/components/ui/CategoryFilterPills.tsx` 改为通用组件：新增 `active`/`items`/`colorFor`/`allLabel` props，不传 `items` 时仍默认使用公文类型分类。
  - `src/app/(dashboard)/hotspots/page.tsx` 传入 `items={cols}`，pill 现在显示从数据中提取的真实栏目。
  - `src/app/(dashboard)/documents/page.tsx` 与 `trash/page.tsx` 改用新 prop `active`（原 `activeCat`）。
- **验证**：`tsc --noEmit` 通过，已 git push。

## 身份认证系统开发计划（待用户确认）

- 按用户要求未直接开发，仅在 `TODO.md` 新增详细可选方案。
- 推荐方案 A：保持现有自定义 JWT Cookie 体系，逐步扩展。
- 规划 5 阶段：
  1. 注册/登录页统一
  2. 找回密码/重置密码
  3. Magic Link 免密登录
  4. 邮箱/手机验证码登录
  5. GitHub / Google 社交登录（登录页圆形图标）
- 列出所需新增表、环境变量、成本/依赖提醒、推荐执行顺序。
